{
  "name": "galeria-holding",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CRM de prospecção B2B da Galeria Holding + agentes de WhatsApp.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'Build OK'",
    "test": "cd agent && npm test",
    "typecheck": "cd agent && npm run typecheck"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  }
}
